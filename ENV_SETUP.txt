# Copy this content to .env.local file
# Create a new file named .env.local in the root directory and paste this:

# WARNING: REGENERATE these keys in your Supabase dashboard before deploying!
# The keys previously in this file have been invalidated for security.

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# IMPORTANT: Never commit .env.local to git!
# Add .env.local to .gitignore (it should already be there)
